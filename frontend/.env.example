# In production (Vercel / Netlify / static host), set this to your Render backend URL.
# In local   development, leave this unset — Vite's proxy handles /api automatically.
VITE_API_URL=https://raven-m0cy.onrender.com
